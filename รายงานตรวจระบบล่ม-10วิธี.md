# รายงานตรวจระบบล่ม CASONPOWER - 10 วิธี

วันที่ตรวจ: 2026-07-29

## สรุปผล

- Server Python syntax: ผ่าน
- Firmware PlatformIO build: ผ่าน
- Chaos/unit test ฝั่ง server: ผ่าน 14/14
- RAM firmware: ใช้ประมาณ 14.4%
- Flash firmware: ใช้ประมาณ 30.7%

## วิธีที่ใช้พยายามทำให้ระบบล่ม และแนวทางป้องกัน

1. ใส่ LINE token ผิดรูปแบบ มีบรรทัดอื่นปน
   - ผล: ระบบเดิมมีโอกาสส่ง LINE ไม่ได้
   - ป้องกันแล้ว: server อ่าน token เฉพาะบรรทัดแรก และซ่อน token ใน error

2. ให้ error message มี Bearer token หลุดออกมา
   - ผล: เสี่ยงข้อมูลลับหลุดใน /health หรือ log
   - ป้องกันแล้ว: sanitize/redact token ก่อนแสดงผล

3. ส่งคำสั่ง LINE แปลกหรืออันตราย
   - ผล: ไม่รับคำสั่งนอก whitelist
   - ป้องกันแล้ว: รับเฉพาะ STATUS, TEST, ON, OFF, RESET, CHECK, WIFI_RESET

4. ส่ง webhook LINE ที่ signature ผิด
   - ผล: server ปฏิเสธ HTTP 401
   - ป้องกันแล้ว: ตรวจ X-Line-Signature ด้วย Channel Secret

5. ส่งข้อความ LINE ยาวมาก
   - ผล: อาจเกิน limit การส่งข้อความ
   - ป้องกันแล้ว: ตัดข้อความไม่เกิน 5000 ตัวอักษร

6. ไฟล์ line_users.json เสียหรือเป็น JSON ผิด
   - ผล: server ไม่ล่ม อ่านเป็น users ว่างแทน
   - แนวทางเสริม: สำรองไฟล์ผู้ใช้ก่อนแก้มือทุกครั้ง

7. มี userId แปลกปนใน line_users.json
   - ผล: server กรองเฉพาะ userId ที่ขึ้นต้นด้วย U
   - ป้องกันแล้ว: invalid id ไม่ถูกใช้งาน

8. ส่งคำสั่งเข้าเร็วมากจน command id ซ้ำ
   - ผลเดิม: มีโอกาส id ซ้ำในมิลลิวินาทีเดียวกัน
   - ป้องกันแล้ว: เพิ่ม uuid ต่อท้าย command id

9. ยิงคำสั่งพร้อมกันหลาย thread ทำให้ไฟล์ command_queue.json เสีย
   - ผลเดิม: มีความเสี่ยงไฟล์ state ชนกัน
   - ป้องกันแล้ว: ใช้ lock และ atomic write สำหรับไฟล์ state

10. ESP32 หายหรือไฟดับนานเกิน timeout
   - ผล: watchdog แจ้งเตือน LINE
   - ป้องกันแล้ว: เมื่อ ESP32 กลับมา online จะส่งข้อความกลับมาอีกครั้ง

## จุดที่ต้องทดสอบภาคสนาม

1. ปิดไฟ ESP32 นานเกิน 3 นาที ต้องได้รับ LINE แจ้ง ESP32 ไม่ตอบสนอง
2. เปิดไฟกลับ ต้องได้รับ LINE แจ้ง ESP32 กลับมาออนไลน์
3. แตะ DI1 ให้ผิดปกติ Relay CH1 ต้อง OFF และไฟแดงติด
4. ปล่อย DI1 กลับปกติ ต้องรอ 30 วินาที แล้ว Relay CH1 ON อัตโนมัติ
5. ส่ง LINE คำว่า CHECK ต้องได้รายงานตรวจระบบ
6. ถอด Wi-Fi/ปิดเราเตอร์ ระบบไฟฟ้าฝั่ง DI/Relay ต้องยังทำงานเองได้

## หมายเหตุสำคัญ

ระบบนี้ช่วยแจ้งเตือนและควบคุมผ่าน ESP32/LINE แต่การขายจริงควรมี protection hardware แยก เช่น fuse, breaker, contactor/relay fail-safe และ wiring ที่ไม่ทำให้โหลดหลักพังเมื่อ ESP32 ดับ
